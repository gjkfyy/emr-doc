最最优先解决的5个问题：


1，录患者（显示列表，选中后的效果，编辑患者）。
2，写病历（为当前患者写病历（能选模板），保存关闭后病历显示在列表中，能打开列表中的病历，能删除病历？，写病历时能自动将当前患者的信息加载到模板中。
3，模板元素的维护（增删改），模板维护（增删），模板编辑并保存
4，后台设置中是否有账户管理？没有先不管保证能登录即可。
5，高级搜索、随访如有UI就打到版本中。只要不报错就行。

   